# Dependencies are declared in pyproject.toml.
#
# This file is kept so that `pip install -r requirements.txt` still works, but
# it installs the project itself rather than duplicating the dependency list —
# two lists drift apart, and the runtime/dev split is what keeps pytest, black,
# and flake8 out of the production image.
#
#   pip install -e .          runtime only
#   pip install -e ".[dev]"   runtime + test and lint tooling
#
-e .
